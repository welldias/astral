#pragma once

// Horizontal alignment of a slide's text — controlled by the "align"
// parameter of the slide marker (see slides/slide_splitter.h). Default: Center.
enum class TextAlign {
    Left,
    Center,
    Right,
};
